#include <iostream>

using namespace std;

int main(){

    // Keywords in C++ are the collection of reserved words and predefined identifiers. These are written in lower cases and
    // have a special meaning defined by the compiler. There are 95 keywords in C++, of which around 30 are unavailable in
    // the C language. These are always used for a particular purpose in a program, but we can’t use them as variable or
    // function names. Some examples of Keywords in C++ are break, int, void, public, private, auto, return, and many more.
    
    return 0;
}