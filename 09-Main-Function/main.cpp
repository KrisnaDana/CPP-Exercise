#include <iostream>

using namespace std;

int main(){
    /*The main() function is the central entry point to a C++ program. It has two main jobs. First, it starts up the program’s execution and handles basic setup.
    Second, it provides a place to put functions that perform I/O or manage memory, handling any programs that will run simultaneously*/

    return 0;
}